<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="chemilmakhlouta.crapchatapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="chemilmakhlouta.crapchatapp.application.CrappApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="chemilmakhlouta.crapchatapp.presentation.chats.view.ChatsListActivity"
            android:screenOrientation="portrait" />
        <activity android:name="chemilmakhlouta.crapchatapp.presentation.chats.view.LatestChatsActivity" />
        <activity android:name=".presentation.login.view.LoginActivity" />
        <activity android:name=".presentation.registration.view.RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.chats.view.SelectRecipientActivity" />
    </application>
</manifest>